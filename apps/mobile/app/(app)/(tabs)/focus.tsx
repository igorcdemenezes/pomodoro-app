/**
 * Route file. Expo Router turns every module under `app/` into a route — a
 * colocated test would become one too, and would be bundled into the app — so
 * screens live in `src/` with their tests and are mounted from here.
 */
export { FocusScreen as default } from '../../../src/sessions/focus-screen';
